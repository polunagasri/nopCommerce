name: mydotnet project

on:
  push:
    branches:
      - develop

jobs:
  dotnet_publish:
    runs-on: 'self-hosted'

    steps:
      - name: code checkout
        uses: actions/checkout@v5

      - name: this is mydotnetpublishcommand
        run: dotnet publish -c Release src/Presentation/Nop.Web/Nop.Web.csproj -o demo


     


  